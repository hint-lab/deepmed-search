name: Deploy to Production

on:
  push:
    branches:
      - main  # 主分支自动部署
      - demo-without-gpu  # Demo 分支自动部署
  workflow_dispatch:  # 手动触发

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub (可选)
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 构建镜像（可选：推送到 Docker Hub）
      # - name: Build and push Docker images
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     push: true
      #     tags: yourusername/deepmed-search:latest

      # 部署到服务器
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          script: |
            cd /path/to/deepmed-search
            
            # 根据分支选择配置文件
            if [ "${{ github.ref_name }}" = "demo-without-gpu" ]; then
              export COMPOSE_FILE="docker-compose.demo.yml"
              echo "📦 使用 Demo 配置（无 GPU）"
            else
              export COMPOSE_FILE="docker-compose.yml"
              echo "📦 使用完整配置（含 GPU）"
            fi
            
            # 拉取最新代码
            git fetch origin
            git checkout ${{ github.ref_name }}
            git pull origin ${{ github.ref_name }}
            
            # 使用部署脚本（自动支持 COMPOSE_FILE 环境变量）
            bash scripts/deploy.sh
            
            echo "✅ 部署完成！使用配置: $COMPOSE_FILE"

      - name: Notify deployment status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "🎉 部署成功！"
          else
            echo "❌ 部署失败！"
          fi

      # 可选：发送通知到 Slack/Discord/钉钉等
      # - name: Send notification
      #   if: always()
      #   uses: 8398a7/action-slack@v3
      #   with:
      #     status: ${{ job.status }}
      #     webhook_url: ${{ secrets.SLACK_WEBHOOK }}

