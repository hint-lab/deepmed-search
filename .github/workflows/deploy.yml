name: Build and Deploy (Tencent Cloud)

on:
  push:
    branches:
      - demo-without-gpu  # 只有演示分支自动部署
  workflow_dispatch:  # 手动触发（可用于 main 分支）

env:
  REGISTRY: jpccr.ccs.tencentyun.com
  NAMESPACE: deepmedsearch
  IMAGE_NAME: deepmed-search

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Tencent Cloud Registry
        uses: docker/login-action@v3
        with:
          registry: jpccr.ccs.tencentyun.com
          username: ${{ secrets.TENCENT_REGISTRY_USER }}
          password: ${{ secrets.TENCENT_REGISTRY_PASSWORD }}

      - name: Extract metadata
        id: meta
        run: |
          echo "sha_short=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT
          echo "branch=${{ github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Build and push app image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            jpccr.ccs.tencentyun.com/deepmedsearch/deepmed-search-app:latest
            jpccr.ccs.tencentyun.com/deepmedsearch/deepmed-search-app:${{ github.ref_name }}
            jpccr.ccs.tencentyun.com/deepmedsearch/deepmed-search-app:${{ steps.meta.outputs.sha_short }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push worker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.worker
          push: true
          tags: |
            jpccr.ccs.tencentyun.com/deepmedsearch/deepmed-search-worker:latest
            jpccr.ccs.tencentyun.com/deepmedsearch/deepmed-search-worker:${{ github.ref_name }}
            jpccr.ccs.tencentyun.com/deepmedsearch/deepmed-search-worker:${{ steps.meta.outputs.sha_short }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push markitdown image
        uses: docker/build-push-action@v5
        with:
          context: ./docker/markitdown
          file: ./docker/markitdown/Dockerfile
          push: true
          tags: |
            jpccr.ccs.tencentyun.com/deepmedsearch/deepmed-markitdown:latest
            jpccr.ccs.tencentyun.com/deepmedsearch/deepmed-markitdown:${{ github.ref_name }}
            jpccr.ccs.tencentyun.com/deepmedsearch/deepmed-markitdown:${{ steps.meta.outputs.sha_short }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    
    steps:
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          script: |
            cd /home/deploy/deepmed-search
            
            echo "📥 拉取最新配置文件..."
            git fetch origin
            git checkout ${{ github.ref_name }}
            git pull origin ${{ github.ref_name }}
            
            # Demo 分支：使用预构建镜像（演示环境）
            export COMPOSE_FILE="docker-compose.demo.yml"
            echo "📦 使用 Demo 配置（无 GPU，使用腾讯云预构建镜像）"
            
            # 检查 .env 文件
            echo ""
            echo "🔍 检查环境变量配置..."
            if [ ! -f .env ]; then
              echo "❌ 错误: .env 文件不存在！"
              echo ""
              echo "📝 请在服务器上创建 .env 文件，包含以下必需的环境变量："
              echo "   - POSTGRES_USER"
              echo "   - POSTGRES_PASSWORD"
              echo "   - POSTGRES_DB"
              echo "   - DATABASE_URL"
              echo "   - NEXTAUTH_URL"
              echo "   - NEXTAUTH_SECRET"
              echo "   - ENCRYPTION_KEY"
              echo ""
              echo "📚 参考文档: docs/deployment/DEPLOYMENT_CHECKLIST.md"
              echo ""
              echo "💡 快速创建 .env 文件："
              echo "   cd /home/deploy/deepmed-search"
              echo "   # 生成密钥:"
              echo "   NEXTAUTH_SECRET=\$(openssl rand -base64 32)"
              echo "   ENCRYPTION_KEY=\$(openssl rand -base64 32)"
              echo "   POSTGRES_PASSWORD=\$(openssl rand -base64 16)"
              echo "   # 然后创建 .env 文件并填入这些值"
              exit 1
            else
              echo "✅ .env 文件存在"
              # 检查关键环境变量是否设置
              MISSING_VARS=""
              source .env 2>/dev/null || true
              
              if [ -z "${POSTGRES_USER:-}" ]; then
                MISSING_VARS="${MISSING_VARS} POSTGRES_USER"
              fi
              if [ -z "${POSTGRES_PASSWORD:-}" ]; then
                MISSING_VARS="${MISSING_VARS} POSTGRES_PASSWORD"
              fi
              if [ -z "${POSTGRES_DB:-}" ]; then
                MISSING_VARS="${MISSING_VARS} POSTGRES_DB"
              fi
              if [ -z "${NEXTAUTH_SECRET:-}" ]; then
                MISSING_VARS="${MISSING_VARS} NEXTAUTH_SECRET"
              fi
              if [ -z "${ENCRYPTION_KEY:-}" ]; then
                MISSING_VARS="${MISSING_VARS} ENCRYPTION_KEY"
              fi
              
              if [ -n "$MISSING_VARS" ]; then
                echo "⚠️  警告: 以下环境变量未设置:$MISSING_VARS"
                echo "   容器可能无法正常启动"
              else
                echo "✅ 关键环境变量已配置"
              fi
            fi
            
            # 验证 Docker Compose 配置
            echo ""
            echo "🔍 验证 Docker Compose 配置..."
            if ! docker compose -f $COMPOSE_FILE config > /dev/null 2>&1; then
              echo "❌ Docker Compose 配置无效！"
              echo "查看详细错误："
              docker compose -f $COMPOSE_FILE config
              exit 1
            else
              echo "✅ Docker Compose 配置有效"
            fi
            
            # 检查并登录腾讯云容器镜像服务
            echo ""
            echo "🔐 检查腾讯云容器镜像服务登录状态..."
            # 检查是否已经登录（检查 ~/.docker/config.json）
            if [ -f ~/.docker/config.json ] && grep -q "jpccr.ccs.tencentyun.com" ~/.docker/config.json 2>/dev/null; then
              echo "✅ 检测到已有登录凭证"
            else
              echo "⚠️  未检测到登录凭证，需要手动登录"
              echo ""
              echo "📝 请在服务器上执行以下命令登录："
              echo "   docker login jpccr.ccs.tencentyun.com -u <你的腾讯云账号ID> -p <你的TCR密码>"
              echo ""
              echo "💡 获取凭证："
              echo "   1. 访问: https://console.cloud.tencent.com/tcr"
              echo "   2. 进入「访问管理」→「访问凭证」"
              echo "   3. 生成临时登录密码或使用长期密码"
              echo "   4. 用户名通常是你的腾讯云账号 ID（一串数字）"
              echo ""
              echo "⚠️  如果已登录但仍有问题，请检查凭证是否过期"
              echo ""
              echo "⏭️  继续尝试拉取镜像（如果已登录但检测失败）..."
            fi
            
            echo ""
            echo "📥 拉取最新镜像..."
            if ! docker compose -f $COMPOSE_FILE pull; then
              echo "❌ 镜像拉取失败！"
              echo ""
              echo "请检查："
              echo "  1. 腾讯云容器镜像服务登录状态"
              echo "  2. 镜像是否存在: jpccr.ccs.tencentyun.com/deepmedsearch/deepmed-search-*"
              echo "  3. 网络连接是否正常"
              echo ""
              echo "💡 手动登录命令："
              echo "   docker login jpccr.ccs.tencentyun.com -u <账号ID> -p <密码>"
              exit 1
            fi
            
            echo ""
            echo "🚀 启动服务..."
            if ! docker compose -f $COMPOSE_FILE up -d; then
              echo "❌ 服务启动失败！"
              echo ""
              echo "查看容器状态："
              docker compose -f $COMPOSE_FILE ps -a
              echo ""
              echo "查看最近日志："
              docker compose -f $COMPOSE_FILE logs --tail=50
              exit 1
            fi
            
            echo ""
            echo "⏳ 等待服务启动（10秒）..."
            sleep 10
            
            echo ""
            echo "📊 服务状态:"
            docker compose -f $COMPOSE_FILE ps
            
            # 检查是否有容器退出
            echo ""
            echo "🔍 检查容器健康状态..."
            EXITED_COUNT=$(docker compose -f $COMPOSE_FILE ps | grep -c "Exit" || echo "0")
            if [ "$EXITED_COUNT" -gt 0 ]; then
              echo "⚠️  警告: 有 $EXITED_COUNT 个容器已退出！"
              echo ""
              echo "已退出的容器："
              docker compose -f $COMPOSE_FILE ps | grep "Exit" || true
              echo ""
              echo "查看退出容器的日志："
              docker compose -f $COMPOSE_FILE ps | grep "Exit" | awk '{print $1}' | while read container; do
                if [ -n "$container" ]; then
                  echo "--- $container 日志 ---"
                  docker logs --tail=30 "$container" 2>&1 || true
                fi
              done
              echo ""
              echo "💡 提示: 检查日志以了解容器退出的原因"
            else
              echo "✅ 所有容器运行中"
            fi
            
            echo ""
            echo "🧹 清理未使用的镜像..."
            docker image prune -f
            
            echo ""
            echo "✅ 部署完成！使用配置: $COMPOSE_FILE"

      - name: Notify deployment status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "🎉 部署成功！"
          else
            echo "❌ 部署失败！"
          fi

      # 飞书通知
      - name: Send Feishu notification
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            STATUS_COLOR="green"
            STATUS_TEXT="✅ 部署成功"
            STATUS_EMOJI="✅"
          else
            STATUS_COLOR="red"
            STATUS_TEXT="❌ 部署失败"
            STATUS_EMOJI="❌"
          fi
          
          curl -X POST "${{ secrets.FEISHU_WEBHOOK_URL }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"msg_type\": \"interactive\",
              \"card\": {
                \"header\": {
                  \"title\": {
                    \"content\": \"$STATUS_EMOJI DeepMed Search 部署通知\",
                    \"tag\": \"plain_text\"
                  },
                  \"template\": \"$STATUS_COLOR\"
                },
                \"elements\": [
                  {
                    \"tag\": \"div\",
                    \"fields\": [
                      {
                        \"is_short\": true,
                        \"text\": {
                          \"tag\": \"lark_md\",
                          \"content\": \"**状态**\\n$STATUS_TEXT\"
                        }
                      },
                      {
                        \"is_short\": true,
                        \"text\": {
                          \"tag\": \"lark_md\",
                          \"content\": \"**分支**\\n\`${{ github.ref_name }}\`\"
                        }
                      },
                      {
                        \"is_short\": true,
                        \"text\": {
                          \"tag\": \"lark_md\",
                          \"content\": \"**提交者**\\n${{ github.actor }}\"
                        }
                      },
                      {
                        \"is_short\": true,
                        \"text\": {
                          \"tag\": \"lark_md\",
                          \"content\": \"**提交信息**\\n${{ github.event.head_commit.message }}\"
                        }
                      }
                    ]
                  },
                  {
                    \"tag\": \"action\",
                    \"actions\": [
                      {
                        \"tag\": \"button\",
                        \"text\": {
                          \"tag\": \"plain_text\",
                          \"content\": \"查看详情\"
                        },
                        \"type\": \"primary\",
                        \"url\": \"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"
                      }
                    ]
                  }
                ]
              }
            }"

